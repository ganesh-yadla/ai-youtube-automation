__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Frontend (future)
node_modules/
.next/

# Editors / OS
.vscode/
.idea/
.DS_Store

# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json

# Generated media (audio, video, thumbnails) - not source, not committed
backend/media/

# Downloaded local model weights (Piper voices, etc.) - large binaries,
# fetched via `python -m piper.download_voices`, not source
backend/models/

# YouTube OAuth credentials - real secrets, never committed
backend/client_secrets.json
backend/token.json
